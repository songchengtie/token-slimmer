{
  "name": "token-slimmer",
  "version": "0.2.0",
  "description": "Token observability and compression proxy for LLM agents",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "bench": "node bench/bench.js",
    "bench:corpus": "node bench/corpus.js",
    "eval:modes": "node bench/corpus.js",
    "lint": "node --check server.js && node --check lib/slimmer.js && node --check bench/bench.js && node --check bench/corpus.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
