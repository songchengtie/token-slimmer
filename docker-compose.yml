version: '3'
services:
  token-slimmer:
    build: .
    ports:
      - "3999:3999"
    environment:
      - HOST=0.0.0.0
      - UPSTREAM_URL=http://host.docker.internal:3000
      - MODE=safe
    restart: unless-stopped
